name: PR builder

on:
  pull_request:
    branches:
      - 'master'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup JDK
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: 'temurin'

      - name: Build
        run: |
          mvn install -DskipTests --batch-mode --update-snapshots -Pcheckstyle

      - name: Findbugs
        run: |
          mvn compile -Pfindbugs --batch-mode

      - name: Verify
        run: |
          mvn verify --batch-mode -Dmaven.test.failure.ignore=false
